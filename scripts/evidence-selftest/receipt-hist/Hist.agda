-- FIXTURE: a receipt above a statement that is now a DEFINITION, not a
-- postulate.  Marker must be `PROBED-HISTORICAL`; plain `PROBED` is a finding,
-- and this is the row that fires at the moment of discharge.
module Hist where

-- PROBED 2026-01-01: covered the two hot shapes.
was-a-postulate : Set
was-a-postulate = Set
