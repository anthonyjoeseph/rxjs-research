-- FIXTURE: a receipt above a statement that is now a DEFINITION, not a
-- postulate.  A receipt has one legal tense, so this one is DELETED rather
-- than re-marked, and this is the row that fires at the moment of discharge.
module Hist where

-- PROBED: covered the two hot shapes.
was-a-postulate : Set
was-a-postulate = Set
