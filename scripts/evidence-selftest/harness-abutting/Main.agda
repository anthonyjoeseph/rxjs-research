module Main where

-- SERIES A — a header running straight into the next one, no code between.
-- SERIES B — the second, whose target must not be credited to the first.
--
-- TARGET: live-one
row : Nat
row = 0
