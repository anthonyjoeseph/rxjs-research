-- FIXTURE: PROSE about the convention, which must never be read as a receipt.
-- Neither line below OPENS its comment with the marker, and that is the whole
-- discriminator -- several real files discuss the word this way.
module Prose where

-- A statement like this cannot be PROBED, because the family is sealed.
-- "PROBED" by an independent route is not a receipt either.
postulate
  live-one : Set
