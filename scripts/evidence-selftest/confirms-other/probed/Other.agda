
-- E7's third, in the form the whitelist caught only by accident: the body
-- does not hand the TARGET back, it discharges the row out of a DIFFERENT
-- unproven statement -- so the row is evidence for one postulate exactly as
-- far as another one is true.
-- TARGET: live-one @b6f6f3
module Other where
row : Confirms (live-one 3)
row = weaken (live-two prog 0)
