-- A fork that only CLAIMS to choose: the header names two candidates and the
-- file proves nothing about them, which is the shape E6 exists to refuse.
-- FORK: live-one
module ForkUnproven where
rows : Set₁
rows = Set
