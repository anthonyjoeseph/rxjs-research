# fixture — a tier whose OPEN QUESTIONS section is well formed, and a second
# tier carrying none at all.  The section is optional when absent and validated
# when present, so this file must PASS: three questions is the cap and not a
# quota, and a tier with nothing unified to ask is silent rather than filled.

## Tier 0 — anchor

### Big picture tier roadmap

- **the anchor group** — the worst-class row and the two statements that share
  its currency; one unit of work because a restatement of any of them moves the
  other two.
- **the mechanical shelf** — every row whose shape is already known, ground in
  one pass off a single loaded context.
- **the parked remainder** — what is left once the two legs above land, kept
  here so the tier's plan is complete rather than open-ended.

### Open questions

- **DOES THE PREDICATE REACH THE QUANTITY IT BOUNDS?** Two rows fail in one
  place, and the shared uncertainty is whether the reading exists to be
  transported at all.
  relevant: `a-falsity`, `b-falsity`
- **A QUESTION MAY NAME MANY ROWS, AND THE LEDGER LINE IS FREE.** Naming is not
  charged, so a question is never shortened by dropping a postulate from its
  list — the same asymmetry the rows carry.
  relevant: `a-falsity`, `b-falsity`, `c-falsity`
- **A WRAPPED LEDGER LINE IS STILL LEDGER.** The continuation below carries
  only names and separators, so it must be read as the relevant list and not
  charged as prose — the silent half being the one that matters.
  relevant: `b-falsity`,
  `c-falsity`

### The ledger

- **`a-falsity`** — FALSITY, `REFUTED, PROBED`: worst class goes first.
- **`b-falsity`** — FALSITY, `NO EVIDENCE`: nothing has instantiated it.
- **`c-falsity`** — FALSITY, `REFUTED`: a witness against the free form.
- **`d-shape`** — SHAPE, `NO EVIDENCE`: a restatement is owed.
- **`e-difficulty`** — DIFFICULTY, `PROBED`: true, correctly stated, hard.

## Tier 1 — no questions

### Big picture tier roadmap

- **the anchor group** — the worst-class row and the two statements that share
  its currency; one unit of work because a restatement of any of them moves the
  other two.
- **the mechanical shelf** — every row whose shape is already known, ground in
  one pass off a single loaded context.
- **the parked remainder** — what is left once the two legs above land, kept
  here so the tier's plan is complete rather than open-ended.

### The ledger

- **`f-falsity`** — FALSITY, `REFUTED`: a witness against the free form.
- **`g-shape`** — SHAPE, `NO EVIDENCE`: a restatement is owed.
- **`h-difficulty`** — DIFFICULTY, `PROBED`: true, correctly stated, hard.
- **`i-grindable`** — GRINDABLE, `TWIN`: the shape is already known.
