# fixture — `ev-tier.md` with the ORDINARY tier's DIFFICULTY row stripped of its
# evidence field.  It must FAIL.  An exemption that spread from the tier carrying
# the marker to the file carrying that tier would be the expensive failure —
# every ordinary tier in a file that happens to schedule one experiment would go
# unchecked, and nothing would say so — so the marker is pinned as a property of
# the TIER here, and the row that must be reported is in the tier that does not
# carry it.

## Tier 0 — does the mechanism work (EVIDENCE TIER)

### Big picture tier roadmap

- **the decisive experiment** — the one construction that can return a NO, built
  first because every other row is worth nothing if it does.
- **the coverage extension** — the shapes the fragment cannot express today,
  which is where both previous silences came from.
- **the transport law** — whether the measure survives an arbitrary body, which
  is what the two legs above are jointly instrumenting.

### Open questions

- **CAN THE MECHANISM CARRY THE EDGE?** Two experiments are jointly waiting on
  one answer, and neither is worth reading until it exists.
  relevant: `ev-alpha`, `ev-beta`

### The ledger

- **`ev-alpha`** — FALSITY: the decisive construction; it can return a NO.
- **`ev-beta`** — FALSITY: the coverage extension the fragment is missing.
- **`ev-gamma`** — DIFFICULTY: the transport law, once the two above land.

## Tier 1 — the ordinary kind

### Big picture tier roadmap

- **the anchor group** — the worst-class row and the two statements that share
  its currency; one unit of work because a restatement of any of them moves the
  other two.
- **the mechanical shelf** — every row whose shape is already known, ground in
  one pass off a single loaded context.
- **the parked remainder** — what is left once the two legs above land, kept
  here so the tier's plan is complete rather than open-ended.

### The ledger

- **`a-falsity`** — FALSITY, `REFUTED, PROBED`: worst class goes first.
- **`b-falsity`** — FALSITY, `NO EVIDENCE`: nothing has instantiated it.
- **`c-falsity`** — FALSITY, `REFUTED`: a witness against the free form.
- **`d-shape`** — SHAPE, `NO EVIDENCE`: a restatement is owed.
- **`e-difficulty`** — DIFFICULTY: true, correctly stated, hard.
