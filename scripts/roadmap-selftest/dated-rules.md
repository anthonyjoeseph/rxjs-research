# Fixture — a rules file carrying dates

Stands in for CLAUDE.md, which is held to the date rule for a different
reason than the roadmap is: the roadmap must be CURRENT, a rules file must be
TIMELESS.  This fixture has no `## Tier` sections and no rows at all, which
is the point — the date scan must fire on a file the row checks cannot even
parse.

**A RULING (Anthony, 2026-08-20).** The attribution earns its keep and the
timestamp does not.

Measured 2026-08-11 at ~120 modules per switch — a figure whose home is the
timings file.
