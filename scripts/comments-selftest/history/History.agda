module History where

-- WHAT THIS LEAF OWES.  The marker below is historical by definition and
-- carries no date, so only the name check can see it.
-- RESTATED after a machine refutation, and the restatement is the one
-- that is stated here.
postulate leaf : Set
