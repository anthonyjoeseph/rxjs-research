module Echo where

-- THE MUST-FIRE DIRECTION FOR THE FIFTH CHECK.  This explanation tells you
-- there is a refutation, which the `REFUTED` section two lines down already
-- says — and says resolvably, since that name is checked.  The prose copy is
-- the one that will still be here after the refutation is retargeted.
-- REFUTED: `caps-frame-boundary-absurd`.
postulate echoes : Set
