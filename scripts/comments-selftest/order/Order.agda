module Order where

-- WHAT THIS LEAF OWES.  The two evidence sections below are in the wrong
-- order: coverage comes before what has been ruled out.
-- PROBED: every clause of the currency.
-- REFUTED: `Refuted.Depth-Hop.depth-hop-∀V-absurd`.
postulate leaf : Set
