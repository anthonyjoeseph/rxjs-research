-- THE PROBE TREE'S APPARATUS: the one module here that states no rows,
-- and the one `make evidence-check` reads as such by name.
--
-- THE TIE BETWEEN A RECEIPT'S ROWS AND THE STATEMENT THEY ARE EVIDENCE
-- ABOUT.  A probe used to restate its target's predicate by hand -- a
-- local `held d = regP? (λ p → …) …` -- and pin THAT by `refl`, so
-- nothing held the row to the postulate: a predicate mistyped, or one
-- quietly weaker than the statement's, stayed green and earned a
-- `PROBED:` receipt for a claim nobody had instantiated.
--
-- `Confirms` closes that.  It takes the target postulate APPLIED at the
-- probe's own concrete arguments and returns that application's type,
-- so the row's type is generated by Agda from the statement as it now
-- reads -- the probe cannot write the claim, only choose the point.
-- The argument is IRRELEVANT (the dot): it exists to fix the type and
-- can never be used, which is the point of passing the postulate at
-- all.  What inhabits the row is then whatever inhabits the statement
-- at that point, and `make evidence-check` (E7) refuses a body that
-- names a POSTULATE -- so the row stands on what this tower has proven,
-- and the statement it is evidence about cannot be its own witness.
--
-- THAT IS A LAUNDERING TEST AND NOT A COMPUTATION TEST, and the
-- difference is what makes the rule satisfiable at all.  A conclusion
-- denominated in a family this tower SEALS for cost does not reduce at
-- any point whatever, so a body held to a numeral could never be
-- written against one and the coverage could never be claimed.  A
-- weakening through a proven inequality does reach such a statement,
-- and is a stronger receipt than a numeral rather than a weaker one.
--
-- A sub-claim is reachable by the statement's OWN eliminators: project
-- a conjunct, apply a ∀ at a point, read a field of a record conclusion.
-- E7 requires the head under those eliminators to be a declared
-- `-- TARGET:`, since an arbitrary function applied to the postulate
-- could return any type at all and the tie would be gone.
--
-- WHAT THIS MODULE USED TO ALSO HOLD BESIDE THE TWO BELOW.  A
-- computable floor under the sealed caps grant that the nesting rows
-- read their figures against is apparatus for a face this development
-- no longer has: the grant, the nest store and the walk maximum it was
-- written over went with the budget.
--
-- RECOVERY: git show 919f115:agda/evidence/probed/Probed/Apparatus.agda
module Probed.Apparatus where

open import Data.Nat using (_<_)
open import Data.Nat.Properties using (_<?_)
open import Relation.Binary.PropositionalEquality using (_≡_)
open import Relation.Nullary using (¬_)
open import Relation.Nullary.Decidable using (True; toWitness)

-- A CONDITIONED TARGET'S PREMISE, DECIDED RATHER THAN ASSERTED.  A
-- statement restated onto a hypothesis can only be applied at a point
-- that SATISFIES it, so a row instantiating one has to discharge the
-- premise — and discharging it is the half of the row worth having,
-- since it is what says the hypothesis is not vacuous over reachable
-- states.  Where the premise is an arithmetic comparison of two
-- computable readings, the decision procedure is the honest witness:
-- it reduces at the probe's own point, so a premise that is FALSE
-- there leaves the implicit unsolvable and the row goes red, which is
-- exactly the refutation the conditioning was supposed to expose.
Below : ∀ {m n} → {t : True (m <? n)} → m < n
Below {t = t} = toWitness t

Confirms : {A : Set} → .(claim : A) → Set
Confirms {A} _ = A

-- THE FORK'S PRODUCT, AND IT IS A TYPE RATHER THAN A MARKER.  A
-- `-- FORK:` probe stands between two candidate MECHANISMS, and what it
-- earns is that they DISAGREE -- prose saying so is a claim no machine
-- reads, and a `-- TARGET:` receipt written from such a file would
-- report coverage the separating rows never bought.
--
-- The candidates are two definitions of ONE signature, so the
-- disagreement is a value: `at` names the point and `apart` carries it
-- there.  When the two agree, `apart` is uninhabited at every point and
-- the record cannot be written at all -- which is what makes the marker
-- decide only WHICH law applies and leaves Agda to decide whether the
-- claim is true.
record Separates {A : Set} {B : A → Set} (f g : (x : A) → B x) : Set where
  constructor separates-at
  field
    at    : A
    apart : ¬ (f at ≡ g at)
