-- README SEMANTICS PROOFS: the root README.md's semantics-by-edge-case,
-- each as a top-line result checked against the Agda spec — one-subscribe-
-- one-batch, each .next() its own instant, cascades inherit their trigger,
-- completion cascades, share connect-no-replay, late-join growth, take
-- counts values, batch order = delivery order, …
--
-- QUEUED: to be filled in as postulates first, then discharged. See
-- memory/global-oracle-plan.md.
module Formal-Verification.Readme-Semantics where
